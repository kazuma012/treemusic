<div class="container">
	<h1>ジャンル編集</h1>
		<%= form_for [:admin,@genre] do |f| %>
			<%= f.label :ジャンル %>
			<%= f.text_field :name %>
			<%= f.submit '変更を保存する', class: "btn btn-primary" %>
		<% end %>
</div>