$(function () {
$(document).on("click", ".js-edit-comment-button", function () {
    const commentId = $(this).data('comment-id');                                
    const commentLabelArea = $('#js-comment-label-' + blog_commentId);   
    const commentTextArea = $('#js-textarea-comment-' + blog_commentId); 
    const commentButton = $('#js-comment-button-' + blog_commentId);
    const commentError = $('#js-comment-post-error-' + blog_commentId);    

    commentLabelArea.show(); 
    commentTextArea.hide();  
    commentButton.hide();
    commentError.hide();
  });
});